# requirements.txt - v2 with dependency conflict resolution

# Core Framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Data Validation and Settings
pydantic==2.7.3
pydantic-settings==2.3.4
python-dotenv==1.0.1

# Logging
structlog==24.2.0

# --- DEPENDENCY CONFLICT RESOLUTION ---
#
# The conflict is between grpcio-tools (requires protobuf < 4) and
# sentiric-contracts-py (requires protobuf >= 6.32.0).
#
# Solution:
# 1. Force the protobuf version required by our internal package (contracts).
# 2. Upgrade grpcio to a newer version that supports a modern protobuf.
#    Version 1.64.1 of grpcio is compatible with protobuf >= 4.25.0.

protobuf==6.32.0
grpcio==1.64.1

# Vector DB Client
qdrant-client==1.9.0

# Embedding Model
sentence-transformers==2.7.0

# HTTP Client
httpx==0.27.0

# Git-based dependency for Contracts
sentiric-contracts-py @ git+https://github.com/sentiric/sentiric-contracts.git@195fc910ff10d2bd9651bd251ba0480b809a03e5