# requirements.txt - v3 letting pip resolve conflicts

# Core Framework
fastapi
uvicorn[standard]

# Data Validation and Settings
pydantic
pydantic-settings
python-dotenv

# Logging
structlog

# gRPC (version will be resolved by pip)
grpcio

# Vector DB Client
qdrant-client

# Embedding Model
sentence-transformers

# HTTP Client
httpx

# Git-based dependency for Contracts (pointing to the fixed main branch)
sentiric-contracts-py @ git+https://github.com/sentiric/sentiric-contracts.git@main